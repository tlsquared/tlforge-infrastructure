#cloud-config

package_update: true
package_upgrade: true

packages: [git]

fs_setup:
  - device: /dev/sdb
    filesystem: ext4
    partition: none

mounts:
  - [sdb, /mnt/data-volume, auto, defaults]

ansible:
  package_name: ansible-core
  install_method: distro
